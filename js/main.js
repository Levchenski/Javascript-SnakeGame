function mas(els) {
    const a = [4, 5, 6];
    let element = document.querySelectorAll('.item');
    for(let s = 0; s < )
    els(element, a);
}

function callback(elem, arr){
    elem.innerHTML = arr.join('-')
}

mas(callback);